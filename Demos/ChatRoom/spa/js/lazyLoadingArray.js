lazyLoadArray = (function() {
  var LazyLoadArray = function() {
    this.elements_ = [];
  };

  LazyLoadArray.prototype.get = function(index) {
    if (this.elements_[index].loaded) {

    } else {
      io.
    }
  };
})